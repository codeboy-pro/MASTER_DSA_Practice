#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
  // set<int>s;
  // set<int, greater<int>> s;
  // s.insert(10);
  // s.insert(20);
  // s.insert(300);
  // s.insert(50);
  // s.insert(100);
  // s.insert(10);
  // s.insert(210);
  // for(auto it=s.begin();it!=s.end();it++){
  //   cout<<*it<<endl;
  // }
  // search

  // find :- return the iterator
  //  if(s.find(210)!=s.end()){
  //      cout<<"present\n";
  //  }
  //  else{
  //    cout<<"absent\n";
  //  }
  // count:- return the count of how much times the  val is present in set
  //  if(s.count(210)){
  //    cout<<"present";
  //  }else cout<<"absent";
  // delete :- deletes the ele
  // s.erase(210);
  // for (auto it = s.begin(); it != s.end(); it++)
  // {
  //   cout << *it << endl;
  // }

  return 0;
}